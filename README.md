# Chapter 5 (Part 2): Range, Pass, Sum and Factorial in Python

This chapter continues the topic of **Loops in Python** and introduces the powerful `range()` function, the `pass` statement, and common loop-based programs such as finding sums and factorials.

## Topics Covered

### Range Function

The `range()` function is used to generate a sequence of numbers. It is commonly used with `for` loops for controlled repetition.

### Syntax:

```python
range(start, stop, step)



